name: Minecraft Plugin Compiler

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # Stap 1: Haal de code op met de actie die Node 24 ondersteunt
    - name: Checkout repository
      uses: actions/checkout@v4

    # Stap 2: Zet Java 25 op en koppel Maven caching los om fouten te vermijden
    - name: Set up JDK 25
      uses: actions/setup-java@v4
      with:
        java-version: '25'
        distribution: 'temurin'
        cache: 'maven'

    # Stap 3: Bouw de plugin met de verplichte -U vlag om weigeringen te negeren
    - name: Build with Maven
      run: mvn -B clean package -U

    # Stap 4: Sla de gemaakte plugin op als downloadbaar bestand
    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: ServerCore-Plugin
        path: target/ServerCore.jar

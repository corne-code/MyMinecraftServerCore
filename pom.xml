<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://apache.org"
         xmlns:xsi="http://w3.org"
         xsi:schemaLocation="http://apache.org
         https://apache.org">

    <modelVersion>4.0.0</modelVersion>

    <groupId>nl.tricraft</groupId>
    <artifactId>tricraftcore</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>ServerCore</name>
    <description>TriCraft ServerCore plugin v2</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>21</maven.compiler.release>
    </properties>

    <repositories>
        <!-- Officiële Purpur Repository -->
        <repository>
            <id>purpurmc</id>
            <name>Purpur Maven Repository</name>
            <url>https://purpurmc.org</url>
        </repository>

        <!-- PaperMC Repository -->
        <repository>
            <id>papermc</id>
            <name>PaperMC Repository</name>
            <url>https://papermc.io</url>
        </repository>
    </repositories>

    <dependencies>
        <!-- Universele Purpur API Snapshot -->
        <dependency>
            <groupId>org.purpurmc.purpur</groupId>
            <artifactId>purpur-api</artifactId>
            <version>1.21.1-R0.1-SNAPSHOT</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>ServerCore</finalName>

        <plugins>
            <!-- Java compiler plugin -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>21</release>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>

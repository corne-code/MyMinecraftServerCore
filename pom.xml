<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://apache.org"
         xmlns:xsi="http://w3.org"
         xsi:schemaLocation="http://apache.org
         https://apache.org">

    <modelVersion>4.0.0</modelVersion>

    <groupId>nl.tricraft</groupId>
    <artifactId>tricraftcore</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>ServerCore</name>
    <description>TriCraft ServerCore plugin</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>25</maven.compiler.release>
    </properties>

    <repositories>
        <!-- Officiële Purpur Repository -->
        <repository>
            <id>purpurmc</id>
            <name>Purpur Maven Repository</name>
            <url>https://repo.purpurmc.org/snapshots</url>
        </repository>

        <!-- PaperMC Repository (als fallback voor onderliggende platform-APIs) -->
        <repository>
            <id>papermc</id>
            <name>PaperMC Repository</name>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
    </repositories>

    <dependencies>
        <!-- Purpur API voor Minecraft 26.2 -->
        <dependency>
            <groupId>org.purpurmc.purpur</groupId>
            <artifactId>purpur-api</artifactId>
            <version>[26.2.build,)</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>ServerCore</finalName>

        <plugins>
            <!-- Java compiler plugin ingesteld op Java 25 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.14.1</version>
                <configuration>
                    <release>25</release>
                </configuration>
            </plugin>

            <!-- JAR packaging configuratie met Paperweight namespace -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.4.2</version>
                <configuration>
                    <archive>
                        <manifestEntries>
                            <paperweight-mappings-namespace>mojang</paperweight-mappings-namespace>
                        </manifestEntries>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
